# 🚀 CommandX

<div align="center">

**Интеллектуальная панель для управления серверами**

[![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)](https://www.python.org/)
[![Django](https://img.shields.io/badge/Django-5.0-green.svg)](https://www.djangoproject.com/)
[![React](https://img.shields.io/badge/React-18.2-blue.svg)](https://reactjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.2-blue.svg)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

</div>

---

## 📋 Описание

**CommandX** — это современная веб-платформа для управления серверами через удобный интерфейс. Проект позволяет подключаться к серверам по SSH, выполнять команды, управлять файлами, устанавливать пакеты и мониторить состояние серверов в реальном времени. Встроенный AI-ассистент помогает решать задачи и отвечает на вопросы по управлению серверами.

### ✨ Основные возможности

- 🔐 **Безопасная аутентификация** через JWT токены
- 🖥️ **Управление серверами** — добавление, редактирование, удаление SSH-серверов
- 💻 **Выполнение команд** через веб-интерфейс в реальном времени
- 📁 **Файловый менеджер** с поддержкой создания, редактирования, удаления файлов
- 📦 **Менеджер пакетов** для установки и управления библиотеками
- 📊 **Мониторинг** — отслеживание CPU, памяти, uptime и других метрик
- 🤖 **AI-ассистент** для помощи в работе с серверами
- 💰 **Система подписок** и виртуальных серверов
- 👤 **Профиль пользователя** с настройками и балансом

---

## 🛠️ Технологический стек

### Backend
- **Django 5.0** — веб-фреймворк
- **Django REST Framework** — API
- **JWT Authentication** — аутентификация
- **Paramiko** — SSH-подключения
- **OpenAI API** — AI-ассистент
- **PostgreSQL/SQLite** — база данных

### Frontend
- **React 18** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик
- **React Router** — маршрутизация
- **Axios** — HTTP клиент
- **Recharts** — графики и визуализация
- **Prism.js** — подсветка синтаксиса

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10+
- Node.js 18+
- npm или yarn
- Git

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/yourusername/commandx.git
   cd commandx
   ```

2. **Настройте Backend**

   ```bash
   # Создайте виртуальное окружение
   python3 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows

   # Установите зависимости
   cd backend
   pip install -r requirements.txt

   # Настройте переменные окружения
   cp ../.env.example ../.env
   # Отредактируйте .env файл

   # Примените миграции
   python manage.py migrate

   # Создайте суперпользователя
   python manage.py createsuperuser

   # Запустите сервер
   python manage.py runserver
   ```

   Backend будет доступен на `http://localhost:8000`

3. **Настройте Frontend**

   Откройте **новый терминал**:

   ```bash
   cd frontend
   npm install
   npm run dev
   ```

   Frontend будет доступен на `http://localhost:5173`

### Автоматический запуск

Используйте скрипт для быстрой настройки:

```bash
chmod +x start.sh
./start.sh
```

---

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Django
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS
CORS_ALLOW_ORIGINS=http://localhost:5173

# Database (опционально, по умолчанию SQLite)
DATABASE_URL=postgresql://user:password@localhost:5432/commandx

# OpenAI (для AI-ассистента)
OPENAI_API_KEY=your-openai-api-key
```

---

## 📡 API Endpoints

### Аутентификация
- `POST /api/auth/token/` — Получить JWT токен
- `POST /api/auth/token/refresh/` — Обновить токен
- `POST /api/auth/register/` — Регистрация
- `POST /api/auth/login/` — Вход

### Серверы
- `GET /api/servers/` — Список серверов
- `POST /api/servers/` — Создать сервер
- `GET /api/servers/{id}/` — Детали сервера
- `PUT /api/servers/{id}/` — Обновить сервер
- `DELETE /api/servers/{id}/` — Удалить сервер
- `POST /api/servers/{id}/exec/` — Выполнить команду
- `GET /api/servers/{id}/ls/` — Список файлов
- `GET /api/servers/{id}/stats/` — Статистика сервера

### Файлы
- `GET /api/servers/{id}/read/` — Читать файл
- `POST /api/servers/{id}/write/` — Записать файл
- `POST /api/servers/{id}/upload/` — Загрузить файл
- `DELETE /api/servers/{id}/delete/` — Удалить файл

### AI
- `POST /api/ai/chat/` — Чат с AI-ассистентом

---

## 📁 Структура проекта

```
commandx/
├── backend/                 # Django backend
│   ├── api/                # API приложение
│   │   ├── models.py      # Модели данных
│   │   ├── views.py       # API views
│   │   ├── serializers.py # Сериализаторы
│   │   └── urls.py        # URL маршруты
│   ├── commandx/          # Настройки Django
│   ├── manage.py          # Django CLI
│   └── requirements.txt   # Python зависимости
│
├── frontend/              # React frontend
│   ├── src/
│   │   ├── api/          # API клиенты
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы
│   │   └── hooks/        # React hooks
│   ├── package.json      # Node зависимости
│   └── vite.config.ts    # Vite конфигурация
│
├── .gitignore            # Git ignore правила
├── README.md             # Документация
└── start.sh              # Скрипт запуска
```

---

## 🔒 Безопасность

⚠️ **Важно для production:**

- Используйте сильный `DJANGO_SECRET_KEY`
- Установите `DEBUG=False`
- Настройте `ALLOWED_HOSTS`
- Используйте HTTPS
- Настройте менеджер секретов для SSH ключей
- Реализуйте rate limiting
- Настройте CORS правильно

---

## 🧪 Разработка

### Backend команды

```bash
# Создать миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Запустить shell
python manage.py shell

# Собрать статические файлы
python manage.py collectstatic
```

### Frontend команды

```bash
# Разработка
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview

# Линтинг
npm run lint
```

---

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

---


<div align="center">

**Сделано с ❤️ для упрощения управления серверами**

⭐ Если проект был полезен, поставьте звезду!

</div>
